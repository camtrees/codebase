------------------------------------------------------------------------------------------
-- Table: volunteer_interest_type
--
-- Purpose: The list of volunteer interests
--
-- Create Order:
--		MUST BE BEFORE
--			volunteer_interest
------------------------------------------------------------------------------------------
-- public.volunteer_interest_type definition

-- Drop table

-- DROP TABLE public.volunteer_interest_type;

CREATE TABLE public.volunteer_interest_type (
	id int4 GENERATED ALWAYS AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 2147483647 START 1 CACHE 1 NO CYCLE) NOT NULL, -- The PostGreSQL unique primary key (generated when the record is created)
	interest text NOT NULL, -- The interest a volunteer might be interested in
	CONSTRAINT interest_type_id_not_null NOT NULL id,
	CONSTRAINT interest_type_interest_not_null NOT NULL interest,
	CONSTRAINT volunteer_interest_type_pkey PRIMARY KEY (id)
);
CREATE UNIQUE INDEX volunteer_interest_type_interest_unique ON public.volunteer_interest_type USING btree (interest);
COMMENT ON TABLE public.volunteer_interest_type IS 'Lookup Table - Volunteer interests';

-- Column comments

COMMENT ON COLUMN public.volunteer_interest_type.id IS 'The PostGreSQL unique primary key (generated when the record is created)';
COMMENT ON COLUMN public.volunteer_interest_type.interest IS 'The interest a volunteer might be interested in';

-- Permissions

ALTER TABLE public.volunteer_interest_type OWNER TO neondb_owner;
GRANT ALL ON TABLE public.volunteer_interest_type TO neondb_owner;