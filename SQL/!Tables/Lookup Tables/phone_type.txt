------------------------------------------------------------------------------------------
-- Table: phone_type
--
-- Purpose: The list of types of phone numbers
--
-- Create Order:
--		MUST BE BEFORE
--			volunteer_phone
------------------------------------------------------------------------------------------
-- public.phone_type definition

-- Drop table

-- DROP TABLE public.phone_type;

CREATE TABLE public.phone_type (
	id int4 GENERATED ALWAYS AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 2147483647 START 1 CACHE 1 NO CYCLE) NOT NULL, -- The PostGreSQL unique primary key (generated when the record is created)
	"label" text NOT NULL, -- Phone type (home, cell, work, etc)
	CONSTRAINT phone_type_id_not_null NOT NULL id,
	CONSTRAINT phone_type_label_not_null NOT NULL label,
	CONSTRAINT phone_type_pkey PRIMARY KEY (id)
);
CREATE UNIQUE INDEX phone_type_label_unique ON public.phone_type USING btree (label);
COMMENT ON TABLE public.phone_type IS 'Lookup Table - Phone Type (home, cell, work, etc)';

-- Column comments

COMMENT ON COLUMN public.phone_type.id IS 'The PostGreSQL unique primary key (generated when the record is created)';
COMMENT ON COLUMN public.phone_type."label" IS 'Phone type (home, cell, work, etc)';

-- Permissions

ALTER TABLE public.phone_type OWNER TO neondb_owner;
GRANT ALL ON TABLE public.phone_type TO neondb_owner;